# Copy this file to `.env.local` and fill in real values.
# `.env.local` is gitignored (via the `*.local` pattern in .gitignore).
# Restart `npm run dev` or rerun `npm run build` after editing.

# Public Airtable form URL for the Provider Finder feedback embed.
# See docs/airtable-setup.md for how to create the form and find its URL.
VITE_AIRTABLE_FORM_URL=https://airtable.com/embed/shrREPLACE_ME
