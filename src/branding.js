// Org identity. Single source of truth — change here, copy updates everywhere.
// User has not finalized the org name or filled in a founder byline; these
// placeholders are intentional.
export const ORG_NAME = '[Org name]'
export const FOUNDER_NAME = '[Founder name]'
